fdfs:
  so-timeout: 1501
  connect-timeout: 601
  thumb-image:             #缩略图生成参数
    width: 150
    height: 150
  tracker-list:            #TrackerList参数,支持多个
    - ${TRACKER_IP:192.168.1.12:22122}
    - ${TRACKER_IP1:192.168.1.14:22122}
  web-server-url: ${FILE_DOWNLOAD:192.168.1.12:8888} #文件下载地址
  pool:
    max-total: 153 #从池中借出的对象的最大数目
    max-wait-millis: 102 #获取连接时的最大等待毫秒数100
server:
  port: 8266
logging:
  config: classpath:logback.xml
tb:
  transport:
    url: ${TB_URL:http://localhost/api/v1} #tb 服务器接口